{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "nimbleBuild",
            "command": "nimble",
            "args": [
                "build",
                "-g",
                "--debugger:native",
                "-o:${workspaceRoot}/src/main"
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "type": "shell"
        },
        {
            "label": "game0",
            "command": "nimble",
            "args": [
                "build",
                "-g",
                "--debugger:native",
                "-o:${workspaceRoot}/bin/game0",
                "game0/main2.nimble"
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "type": "shell"
        }
    ]
}